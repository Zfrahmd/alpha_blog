
    <h1> Showing articles details</h1>
    <p><strong>Title: </strong><%= @article.title%></p>
    <p><strong>Description: </strong><%= @article.description%></p>

    <p style="display:inline;"><%= link_to 'Edit', edit_article_path(@article.id) %> | </p>
    
    <p style="display:inline;"><%= link_to 'Remove', articles_delete_path, method: :delete, data: {confirm: "Are you sure"}%> | </p>
    
    <!--
    <%= button_to 'Delete', article_path(@article.id), method: :delete, data: {confirm: "Are you sure"} 
    <%= button_to 'Delete', article_path(@article.id), method: :delete, data: {confirm: "Are you sure"} %>
    -->
    
    <p style="display:inline;"><%= link_to 'Return to articles', articles_path %></p>

